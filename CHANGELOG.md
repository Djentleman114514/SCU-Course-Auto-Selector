# 修改说明

本文件记录项目的功能变更、关键规则和已完成的验证。以后每次修改功能时，均在顶部追加一条简短记录。

## 记录规则

- 说明改了什么、为什么改，以及是否已在选课页面验证。
- 不记录账号、Cookie、课程个人偏好或其他敏感信息。
- 修复问题时，写明原问题和修复后的规则。

## 2026-09-16

### 分支 `basic-course-flow`：基础选课流程（待真实页面验证）

- 创建不做课程时间分组的独立分支，主分支的模拟分组逻辑保持不变。
- 开启“有课余量的课程”筛选，按用户填写的课程号顺序逐一查询。
- 发现第一门有余量课程后自动勾选、调用页面 `tijiao()` 提交，并仅在服务器返回 `result === "ok"` 时报告成功后停止。
- 用户成功选课后手动从 `courseNumbers` 删除该课程号、重新运行脚本，再开始下一轮监控。
- 提交失败会取消勾选并继续监控；验证码、页面已有勾选或响应超时会暂停并要求用户处理。

### 新增：模拟选择版（待真实页面验证）

- 主脚本改为整轮依次查询全部课程号；模拟阶段会关闭余量筛选，读取全部班次后再统一分类。
- 按同一天内节次区间重叠自动合并时段组；每组按用户 `courseNumbers` 的填写顺序确定模拟选择的第一门课程。
- Console 会列出每组的“将选择”课程和“将停止的同组候选课”；只有课余量大于 0 的班次才会成为“将选择”候选。
- 当前版本严格不勾选、不提交，仅用于验证监控、分类和优先级逻辑。

### 已确认：有余量班次的批量时段分类

- 批量测试已在四川大学自由选课页面运行成功。
- 测试会自动开启“有课余量的课程”筛选，只读取当前实际可选的班次，不会勾选或提交课程。
- 用户只需提供课程号；脚本从页面勾选框的 JSON 数据中读取课序号、课程名、课余量、周次、星期和节次。
- 同一天内节次区间重叠的课程自动归为同一个时段组。例如 `5–7` 与 `5–6`、`1–4` 与 `3–4` 属于同组。

### 课程配置简化

- 用户配置改为只填写 `courseNumbers`，不再手动填写课序号、时间或分组。
- 同一课程号的多个班次由页面数据自动识别。

### 解析修复

- 修正 `cxjc` 字段含义：它表示连续上课节数，不是结束节次。
- 结束节次计算为：`skjc + cxjc - 1`。

### 新增测试工具

- `tests/parse-course-metadata.console.js`：读取当前表格，检查单次页面解析和自动分组结果。
- `tests/classify-course-numbers.console.js`：依次查询一组课程号，分类当前有余量的班次。

### 仓库初始化

- 添加 README、MIT License、`.gitignore` 和课程配置模板。
- 自动勾选和提交功能尚未迁移进当前发布脚本。
